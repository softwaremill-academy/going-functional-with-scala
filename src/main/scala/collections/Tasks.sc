/**
  Write a function called ‘contains’ that takes a List[A] and an item of type A,
  and returns true if the item is one of the members of the list, and false if it isn’t.
*/

def contains[A](list: List[A], item: A): Boolean = ???


/**
Write a function called ‘reverse’ that takes a List and returns the same list in reverse order.
  */

def reverse[A](list: List[A]): List[A] = ???